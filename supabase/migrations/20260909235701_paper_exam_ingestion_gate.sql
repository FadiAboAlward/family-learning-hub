-- Historical Production migration mirror.
-- Identity: 20260909235701_paper_exam_ingestion_gate
-- Classification: actual schema/runtime state requiring canonical Git representation.
-- Equivalent Git identity: none; its gate was superseded, but its persistent unique index remains Production-only.
-- Final state: quiz_versions_paper_model_code_workspace_uidx is owned by a future forward-only reconciliation migration.
-- This file records Production migration identity only. Historical Production SQL must not be replayed.
select 1;
