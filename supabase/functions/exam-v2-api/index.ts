import { createClient } from "npm:@supabase/supabase-js@2";
const SUPABASE_URL=Deno.env.get("SUPABASE_URL")!,SERVICE_ROLE=Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const admin=createClient(SUPABASE_URL,SERVICE_ROLE,{auth:{persistSession:false,autoRefreshToken:false}}),WORKSPACE_ID="55f9224c-8ba7-4cbc-9f88-713e6a6b41df";
function cors(o:string|null){const a=new Set(["https://fadiaboalward.github.io","http://localhost:5173","http://localhost:4173"]);return{"Access-Control-Allow-Origin":o&&a.has(o)?o:"https://fadiaboalward.github.io","Access-Control-Allow-Headers":"authorization, x-client-info, apikey, content-type","Access-Control-Allow-Methods":"POST, OPTIONS","Content-Type":"application/json; charset=utf-8","Vary":"Origin"};}
function json(d:unknown,s=200,o:string|null=null){return new Response(JSON.stringify(d),{status:s,headers:cors(o)});}
function fb(s:string){const n=s.replaceAll("-","+").replaceAll("_","/")+"===".slice((s.length+3)%4),r=atob(n);return new Uint8Array([...r].map(c=>c.charCodeAt(0)));}
function tb(b:Uint8Array){let s="";for(const x of b)s+=String.fromCharCode(x);return btoa(s).replaceAll("+","-").replaceAll("/","_").replaceAll("=","");}
async function hm(d:string){const k=await crypto.subtle.importKey("raw",new TextEncoder().encode(SERVICE_ROLE),{name:"HMAC",hash:"SHA-256"},false,["sign"]);return tb(new Uint8Array(await crypto.subtle.sign("HMAC",k,new TextEncoder().encode(d))));}
async function learner(req:Request){const a=req.headers.get("authorization")||"";if(!a.startsWith("Bearer "))throw new Error("AUTH_REQUIRED");const[b,s]=a.slice(7).trim().split(".");if(!b||!s||await hm(b)!==s)throw new Error("INVALID_SESSION");const p=JSON.parse(new TextDecoder().decode(fb(b)));if(p.typ!=="learner"||p.workspace_id!==WORKSPACE_ID||!p.learner_id)throw new Error("INVALID_SESSION");if(!p.exp||p.exp<Math.floor(Date.now()/1000))throw new Error("SESSION_EXPIRED");return String(p.learner_id);}
async function accessibleQuiz(lid:string,slug:string){const{data:q}=await admin.from("quizzes").select("id,slug,title,description,delivery_config,status").eq("workspace_id",WORKSPACE_ID).eq("slug",slug).eq("status","active").maybeSingle();if(!q)throw new Error("QUIZ_NOT_FOUND");const{data:vs}=await admin.from("quiz_versions").select("id,version_no,settings").eq("workspace_id",WORKSPACE_ID).eq("quiz_id",q.id).eq("state","published").order("version_no",{ascending:false}).limit(1);const v=vs?.[0];if(!v)throw new Error("VERSION_NOT_FOUND");const{data:en}=await admin.from("learner_program_enrollments").select("program_id").eq("workspace_id",WORKSPACE_ID).eq("learner_id",lid).eq("status","active");const pids=(en||[]).map((x:any)=>x.program_id);let viaProgram=false;if(pids.length){const{data:pq}=await admin.from("program_quizzes").select("id").eq("workspace_id",WORKSPACE_ID).eq("quiz_id",q.id).eq("availability","available").in("program_id",pids).limit(1);viaProgram=Boolean(pq?.length);}const{data:qa}=await admin.from("quiz_assignments").select("id,status,available_at,due_at").eq("workspace_id",WORKSPACE_ID).eq("learner_id",lid).eq("quiz_version_id",v.id).in("status",["assigned","in_progress"]).order("created_at",{ascending:false}).limit(1).maybeSingle();let viaAssignment=false;if(qa){const now=Date.now();viaAssignment=(!qa.available_at||new Date(qa.available_at).getTime()<=now)&&(!qa.due_at||new Date(qa.due_at).getTime()>=now);}if(!viaProgram&&!viaAssignment)throw new Error("QUIZ_NOT_AVAILABLE");return{quiz:q,version:v};}
async function qp(ids:string[]){if(!ids.length)return[];const[{data:qs},{data:os},{data:links}]=await Promise.all([admin.from("quiz_questions").select("id,question_code,position,question_type,prompt,origin,source_page_start,source_page_end,points,difficulty_level").eq("workspace_id",WORKSPACE_ID).in("id",ids),admin.from("quiz_question_options").select("id,question_id,position,label,content").eq("workspace_id",WORKSPACE_ID).in("question_id",ids).order("position"),admin.from("quiz_question_assets").select("question_id,asset_id,position,purpose,alt_text").eq("workspace_id",WORKSPACE_ID).in("question_id",ids).order("position")]);const assetIds=[...new Set((links||[]).map((x:any)=>x.asset_id))];let assets:any[]=[];if(assetIds.length){const r=await admin.from("assets").select("id,kind,mime_type,storage_bucket,storage_path,metadata").eq("workspace_id",WORKSPACE_ID).in("id",assetIds);assets=r.data||[];}const am=new Map(assets.map((a:any)=>[a.id,a])),om=new Map<string,any[]>(),lm=new Map<string,any[]>();for(const o of os||[]){if(!om.has(o.question_id))om.set(o.question_id,[]);om.get(o.question_id)!.push({id:o.id,position:o.position,label:o.label,content:o.content});}for(const l of links||[]){if(!lm.has(l.question_id))lm.set(l.question_id,[]);const a=am.get(l.asset_id);if(a){const m=a.metadata||{};lm.get(l.question_id)!.push({position:l.position,purpose:l.purpose,alt_text:l.alt_text,kind:a.kind,mime_type:a.mime_type,url:m.public_url||m.url||null,storage_bucket:a.storage_bucket,storage_path:a.storage_path});}}const qm=new Map((qs||[]).map((q:any)=>[q.id,{...q,options:om.get(q.id)||[],assets:lm.get(q.id)||[]} ]));return ids.map(id=>qm.get(id)).filter(Boolean);}
async function startExam(lid:string,slug:string){const{quiz,version}=await accessibleQuiz(lid,slug);const{data:ex}=await admin.from("quiz_attempts").select("id,started_at").eq("workspace_id",WORKSPACE_ID).eq("learner_id",lid).eq("quiz_version_id",version.id).eq("status","in_progress").eq("delivery_mode","exam").order("started_at",{ascending:false}).limit(1).maybeSingle();let a=ex;const resumed=Boolean(ex);if(!a){const{data:c,error}=await admin.from("quiz_attempts").insert({workspace_id:WORKSPACE_ID,learner_id:lid,quiz_version_id:version.id,status:"in_progress",delivery_mode:"exam",metadata:{engine:"exam-v2-api-v3",quiz_slug:slug,server_graded:true,server_state:true}}).select("id,started_at").single();if(error||!c)throw new Error("ATTEMPT_CREATE_FAILED");a=c;const{data:core}=await admin.from("quiz_questions").select("id,position,difficulty_level").eq("workspace_id",WORKSPACE_ID).eq("quiz_version_id",version.id).eq("delivery_role","core").order("position");const desired=Math.max(1,Number((quiz.delivery_config as any)?.exam?.question_count||10)),sel=(core||[]).slice(0,Math.min(desired,(core||[]).length));if(!sel.length)throw new Error("NO_EXAM_QUESTIONS");const rows=sel.map((q:any,i:number)=>({workspace_id:WORKSPACE_ID,quiz_attempt_id:a!.id,sequence_no:i+1,question_id:q.id,source_role:"core",difficulty_level:q.difficulty_level,status:i===0?"active":"pending",selection_reason:"exam_core_selection"}));const{error:e}=await admin.from("quiz_attempt_question_queue").insert(rows);if(e)throw new Error("QUEUE_CREATE_FAILED");}const{data:queue}=await admin.from("quiz_attempt_question_queue").select("sequence_no,question_id,status,is_flagged").eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",a.id).order("sequence_no"),ids=(queue||[]).map((r:any)=>r.question_id),payload=await qp(ids),pm=new Map(payload.map((q:any)=>[q.id,q]));const{data:saved}=await admin.from("quiz_attempt_answers").select("question_id,response").eq("workspace_id",WORKSPACE_ID).eq("attempt_id",a.id),sm=new Map((saved||[]).map((x:any)=>[x.question_id,x.response]));return{attempt_id:a.id,started_at:a.started_at,resumed,quiz:{slug:quiz.slug,title:quiz.title,description:quiz.description},questions:(queue||[]).map((r:any)=>({...r,question:pm.get(r.question_id),saved_response:sm.get(r.question_id)||null}))};}
async function saveAnswer(lid:string,b:any){const aid=String(b.attempt_id||""),qid=String(b.question_id||""),pos=Number(b.option_position);if(!aid||!qid||!Number.isInteger(pos))throw new Error("INVALID_ANSWER");const{data:a}=await admin.from("quiz_attempts").select("id,status,delivery_mode").eq("workspace_id",WORKSPACE_ID).eq("id",aid).eq("learner_id",lid).maybeSingle();if(!a||a.status!=="in_progress"||a.delivery_mode!=="exam")throw new Error("ATTEMPT_NOT_ACTIVE");const{data:q}=await admin.from("quiz_attempt_question_queue").select("id").eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",aid).eq("question_id",qid).maybeSingle();if(!q)throw new Error("QUESTION_NOT_IN_EXAM");await admin.from("quiz_attempt_answers").upsert({workspace_id:WORKSPACE_ID,attempt_id:aid,question_id:qid,response:{option_position:pos},evaluation:"ungraded",is_correct:null,points_awarded:null,attempts_used:1,hints_used:0,first_try_correct:null,mastery_result:null},{onConflict:"attempt_id,question_id"});return{ok:true,option_position:pos};}
async function setFlag(lid:string,b:any){const aid=String(b.attempt_id||""),qid=String(b.question_id||""),flag=Boolean(b.is_flagged);const{data:a}=await admin.from("quiz_attempts").select("id,status,delivery_mode").eq("workspace_id",WORKSPACE_ID).eq("id",aid).eq("learner_id",lid).maybeSingle();if(!a||a.status!=="in_progress"||a.delivery_mode!=="exam")throw new Error("ATTEMPT_NOT_ACTIVE");const{data:q}=await admin.from("quiz_attempt_question_queue").select("id").eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",aid).eq("question_id",qid).maybeSingle();if(!q)throw new Error("QUESTION_NOT_IN_EXAM");await admin.from("quiz_attempt_question_queue").update({is_flagged:flag}).eq("id",q.id);return{ok:true,is_flagged:flag};}
async function submitExam(lid:string,b:any){const aid=String(b.attempt_id||"");const{data:a}=await admin.from("quiz_attempts").select("id,quiz_version_id,status,started_at").eq("workspace_id",WORKSPACE_ID).eq("id",aid).eq("learner_id",lid).maybeSingle();if(!a||a.status!=="in_progress")throw new Error("ATTEMPT_NOT_ACTIVE");const{data:queue}=await admin.from("quiz_attempt_question_queue").select("question_id,sequence_no,is_flagged").eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",aid).order("sequence_no"),ids=(queue||[]).map((x:any)=>x.question_id);const[{data:ans},{data:keys},{data:qs},{data:v}]=await Promise.all([admin.from("quiz_attempt_answers").select("id,question_id,response").eq("workspace_id",WORKSPACE_ID).eq("attempt_id",aid).in("question_id",ids),admin.from("quiz_question_answer_keys").select("question_id,correct_answer,explanation,correct_explanation,final_incorrect_explanation").eq("workspace_id",WORKSPACE_ID).in("question_id",ids),admin.from("quiz_questions").select("id,question_code,prompt,points").eq("workspace_id",WORKSPACE_ID).in("id",ids),admin.from("quiz_versions").select("quiz_id").eq("workspace_id",WORKSPACE_ID).eq("id",a.quiz_version_id).single()]);if((ans||[]).length!==ids.length)throw new Error("EXAM_NOT_COMPLETE");const km=new Map((keys||[]).map((k:any)=>[k.question_id,k])),qm=new Map((qs||[]).map((q:any)=>[q.id,q])),am=new Map((ans||[]).map((x:any)=>[x.question_id,x])),fm=new Map((queue||[]).map((x:any)=>[x.question_id,Boolean(x.is_flagged)]));let score=0,max=0;const review:any[]=[];for(const id of ids){const x=am.get(id),k=km.get(id),q=qm.get(id);if(!x||!k||!q)throw new Error("EXAM_DATA_INCOMPLETE");const s=Number(x.response?.option_position),c=Number(k.correct_answer?.option_position),ok=s===c,p=Number(q.points||1);max+=p;if(ok)score+=p;await admin.from("quiz_attempt_answers").update({evaluation:ok?"correct":"incorrect",is_correct:ok,points_awarded:ok?p:0,first_try_correct:ok,mastery_result:ok?"mastered":"not_mastered"}).eq("id",x.id);review.push({question_id:id,question_code:q.question_code,prompt:q.prompt,response:x.response,is_correct:ok,was_flagged:fm.get(id)||false,correct_answer:k.correct_answer,explanation:ok?(k.correct_explanation||k.explanation):(k.final_incorrect_explanation||k.explanation)});}const pct=max?Math.round(score/max*10000)/100:0,dur=Math.max(1,Math.round((Date.now()-new Date(a.started_at).getTime())/1000));await admin.from("quiz_attempts").update({status:"submitted",submitted_at:new Date().toISOString(),score_points:score,max_points:max,percentage:pct,duration_seconds:dur,metadata:{engine:"exam-v2-api-v3",server_graded:true,question_count:ids.length,flagged_count:[...fm.values()].filter(Boolean).length}}).eq("id",aid);await admin.from("quiz_attempt_question_queue").update({status:"completed"}).eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",aid);const{data:quiz}=await admin.from("quizzes").select("slug,title").eq("workspace_id",WORKSPACE_ID).eq("id",v!.quiz_id).single();return{ok:true,attempt_id:aid,quiz,score_points:score,max_points:max,percentage:pct,review};}
Deno.serve(async(req:Request)=>{const o=req.headers.get("origin");if(req.method==="OPTIONS")return new Response(null,{status:204,headers:cors(o)});if(req.method!=="POST")return json({error:"METHOD_NOT_ALLOWED"},405,o);try{const lid=await learner(req),b=await req.json().catch(()=>({})),a=String(b.action||"");if(a==="start_exam")return json(await startExam(lid,String(b.quiz_slug||"")),200,o);if(a==="save_answer")return json(await saveAnswer(lid,b),200,o);if(a==="set_flag")return json(await setFlag(lid,b),200,o);if(a==="submit_exam")return json(await submitExam(lid,b),200,o);return json({error:"UNKNOWN_ACTION"},400,o);}catch(e){const m=e instanceof Error?e.message:"SERVER_ERROR",au=["AUTH_REQUIRED","INVALID_SESSION","SESSION_EXPIRED"],nf=["QUIZ_NOT_FOUND","QUIZ_NOT_AVAILABLE","VERSION_NOT_FOUND","NO_EXAM_QUESTIONS"],bad=["INVALID_ANSWER","ATTEMPT_NOT_ACTIVE","QUESTION_NOT_IN_EXAM","EXAM_NOT_COMPLETE"];return json({error:m},au.includes(m)?401:nf.includes(m)?404:bad.includes(m)?400:500,o);}});
