import { createClient } from "npm:@supabase/supabase-js@2";

const SUPABASE_URL = Deno.env.get("SUPABASE_URL")!;
const SERVICE_ROLE = Deno.env.get("SUPABASE_SERVICE_ROLE_KEY")!;
const admin = createClient(SUPABASE_URL, SERVICE_ROLE, { auth: { persistSession: false, autoRefreshToken: false } });
const WORKSPACE_ID = "55f9224c-8ba7-4cbc-9f88-713e6a6b41df";

function cors(origin: string | null) {
  const allowed = new Set(["https://fadiaboalward.github.io", "http://localhost:5173", "http://localhost:4173"]);
  return {
    "Access-Control-Allow-Origin": origin && allowed.has(origin) ? origin : "https://fadiaboalward.github.io",
    "Access-Control-Allow-Headers": "authorization, x-client-info, apikey, content-type",
    "Access-Control-Allow-Methods": "POST, OPTIONS",
    "Content-Type": "application/json; charset=utf-8",
    "Vary": "Origin",
  };
}
function json(data: unknown, status = 200, origin: string | null = null) { return new Response(JSON.stringify(data), { status, headers: cors(origin) }); }
function fromB64url(s: string) { const norm=s.replaceAll("-","+").replaceAll("_","/")+"===".slice((s.length+3)%4),raw=atob(norm);return new Uint8Array([...raw].map(c=>c.charCodeAt(0))); }
function b64url(bytes: Uint8Array) { let s="";for(const b of bytes)s+=String.fromCharCode(b);return btoa(s).replaceAll("+","-").replaceAll("/","_").replaceAll("=",""); }
async function hmac(data: string) { const key=await crypto.subtle.importKey("raw",new TextEncoder().encode(SERVICE_ROLE),{name:"HMAC",hash:"SHA-256"},false,["sign"]);return b64url(new Uint8Array(await crypto.subtle.sign("HMAC",key,new TextEncoder().encode(data)))); }
async function learner(req: Request) { const auth=req.headers.get("authorization")||"";if(!auth.startsWith("Bearer "))throw new Error("AUTH_REQUIRED");const[body,sig]=auth.slice(7).trim().split(".");if(!body||!sig||await hmac(body)!==sig)throw new Error("INVALID_SESSION");const p=JSON.parse(new TextDecoder().decode(fromB64url(body)));if(p.typ!=="learner"||p.workspace_id!==WORKSPACE_ID||!p.learner_id)throw new Error("INVALID_SESSION");if(!p.exp||p.exp<Math.floor(Date.now()/1000))throw new Error("SESSION_EXPIRED");return{learner_id:String(p.learner_id),workspace_id:String(p.workspace_id)}; }

async function accessibleQuiz(learnerId: string, slug: string) {
  const { data: quiz } = await admin.from("quizzes").select("id,workspace_id,slug,title,description,quiz_kind,delivery_config,subject_id,book_id,unit_id,status").eq("workspace_id",WORKSPACE_ID).eq("slug",slug).eq("status","active").maybeSingle();
  if (!quiz) throw new Error("QUIZ_NOT_FOUND");
  const { data: enrollments } = await admin.from("learner_program_enrollments").select("id,program_id").eq("workspace_id",WORKSPACE_ID).eq("learner_id",learnerId).eq("status","active");
  const programIds=(enrollments||[]).map((e:any)=>e.program_id);let viaProgram=false;
  if(programIds.length){const{data:pq}=await admin.from("program_quizzes").select("id,program_id").eq("workspace_id",WORKSPACE_ID).eq("quiz_id",quiz.id).eq("availability","available").in("program_id",programIds).limit(1);viaProgram=Boolean(pq?.length);}
  const { data: versions } = await admin.from("quiz_versions").select("id,version_no,state,settings,question_language,explanation_language,terminology_display_mode").eq("workspace_id",WORKSPACE_ID).eq("quiz_id",quiz.id).eq("state","published").order("version_no",{ascending:false}).limit(1);
  const version=versions?.[0];if(!version)throw new Error("VERSION_NOT_FOUND");
  const { data: assignment } = await admin.from("quiz_assignments").select("id,status,available_at,due_at,max_attempts,learner_program_enrollment_id").eq("workspace_id",WORKSPACE_ID).eq("learner_id",learnerId).eq("quiz_version_id",version.id).in("status",["assigned","in_progress"]).order("created_at",{ascending:false}).limit(1).maybeSingle();
  let viaAssignment=false;if(assignment){const now=Date.now();viaAssignment=(!assignment.available_at||new Date(assignment.available_at).getTime()<=now)&&(!assignment.due_at||new Date(assignment.due_at).getTime()>=now);}
  if(!viaProgram&&!viaAssignment)throw new Error("QUIZ_NOT_AVAILABLE");return{quiz,version,assignment:viaAssignment?assignment:null};
}

async function catalog(learnerId: string) {
  const { data: enrollments }=await admin.from("learner_program_enrollments").select("id,status,is_primary,started_at,program:learning_programs(id,slug,code,title,description,program_type,grade_level,school_year,primary_language,status)").eq("workspace_id",WORKSPACE_ID).eq("learner_id",learnerId).eq("status","active");
  const clean=(enrollments||[]).filter((e:any)=>e.program&&e.program.status==="active"),programIds=clean.map((e:any)=>e.program.id);let offerings:any[]=[];
  if(programIds.length){const{data}=await admin.from("program_quizzes").select("program_id,program_subject_id,sort_order,availability,quiz:quizzes(id,slug,title,description,quiz_kind,delivery_config,subject_id,book_id,unit_id,status)").eq("workspace_id",WORKSPACE_ID).eq("availability","available").in("program_id",programIds).order("sort_order");offerings=(data||[]).filter((x:any)=>x.quiz?.status==="active");}
  const byProgram=new Map<string,any[]>();for(const o of offerings){if(!byProgram.has(o.program_id))byProgram.set(o.program_id,[]);byProgram.get(o.program_id)!.push(o.quiz);}
  return{programs:clean.map((e:any)=>({enrollment_id:e.id,is_primary:e.is_primary,started_at:e.started_at,...e.program,quizzes:byProgram.get(e.program.id)||[]}))};
}

async function questionPayload(ids:string[]){if(!ids.length)return[];const[{data:questions},{data:options}]=await Promise.all([admin.from("quiz_questions").select("id,position,question_type,prompt,origin,source_page_start,source_page_end,source_metadata,points,difficulty_level,max_attempts,remediation_after_attempt,adaptive_enabled,delivery_role,prompt_language,terminology_display_mode").eq("workspace_id",WORKSPACE_ID).in("id",ids),admin.from("quiz_question_options").select("id,question_id,position,label,content").eq("workspace_id",WORKSPACE_ID).in("question_id",ids).order("position")]);const om=new Map<string,any[]>();for(const o of options||[]){if(!om.has(o.question_id))om.set(o.question_id,[]);om.get(o.question_id)!.push({id:o.id,position:o.position,label:o.label,content:o.content});}const qm=new Map((questions||[]).map((q:any)=>[q.id,{...q,options:om.get(q.id)||[]}]));return ids.map(id=>qm.get(id)).filter(Boolean);}

async function startQuiz(learnerId:string,slug:string){const{quiz,version,assignment}=await accessibleQuiz(learnerId,slug);const{data:existing}=await admin.from("quiz_attempts").select("id,started_at").eq("workspace_id",WORKSPACE_ID).eq("learner_id",learnerId).eq("quiz_version_id",version.id).eq("status","in_progress").eq("delivery_mode","learning").order("started_at",{ascending:false}).limit(1).maybeSingle();let attempt=existing;if(!attempt){const{data:created,error}=await admin.from("quiz_attempts").insert({workspace_id:WORKSPACE_ID,learner_id:learnerId,quiz_version_id:version.id,assignment_id:assignment?.id||null,status:"in_progress",delivery_mode:"learning",metadata:{engine:"learning-api-v1",quiz_slug:slug}}).select("id,started_at").single();if(error||!created)throw new Error("ATTEMPT_CREATE_FAILED");attempt=created;const{data:core}=await admin.from("quiz_questions").select("id,position,difficulty_level").eq("workspace_id",WORKSPACE_ID).eq("quiz_version_id",version.id).eq("delivery_role","core").order("position"),ids=(core||[]).map((q:any)=>q.id);let cm=new Map<string,string>();if(ids.length){const{data:links}=await admin.from("quiz_question_concepts").select("question_id,concept_id,is_primary").eq("workspace_id",WORKSPACE_ID).in("question_id",ids).eq("is_primary",true);cm=new Map((links||[]).map((x:any)=>[x.question_id,x.concept_id]));}const queue=(core||[]).map((q:any,i:number)=>({workspace_id:WORKSPACE_ID,quiz_attempt_id:attempt!.id,sequence_no:i+1,question_id:q.id,source_role:"core",concept_id:cm.get(q.id)||null,difficulty_level:q.difficulty_level,status:i===0?"active":"pending",selection_reason:"published_core_question"}));if(queue.length){const{error:qe}=await admin.from("quiz_attempt_question_queue").insert(queue);if(qe)throw new Error("QUEUE_CREATE_FAILED");}}
  const{data:rows}=await admin.from("quiz_attempt_question_queue").select("sequence_no,question_id,source_role,concept_id,difficulty_level,status").eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",attempt.id).order("sequence_no"),payload=await questionPayload((rows||[]).map((r:any)=>r.question_id)),pm=new Map(payload.map((q:any)=>[q.id,q]));return{attempt_id:attempt.id,started_at:attempt.started_at,quiz:{id:quiz.id,slug:quiz.slug,title:quiz.title,description:quiz.description,quiz_kind:quiz.quiz_kind,delivery_config:quiz.delivery_config},version:{id:version.id,version_no:version.version_no,settings:version.settings},queue:(rows||[]).map((r:any)=>({...r,question:pm.get(r.question_id)}))};}

async function maybeAddRemediation(attemptId:string,versionId:string,conceptId:string|null,parentQuestionId:string){if(!conceptId)return null;const{data:links}=await admin.from("quiz_question_concepts").select("question_id").eq("workspace_id",WORKSPACE_ID).eq("concept_id",conceptId),candidateIds=(links||[]).map((x:any)=>x.question_id);if(!candidateIds.length)return null;const{data:candidates}=await admin.from("quiz_questions").select("id,difficulty_level").eq("workspace_id",WORKSPACE_ID).eq("quiz_version_id",versionId).eq("delivery_role","remediation_pool").in("id",candidateIds).order("difficulty_level");if(!candidates?.length)return null;const{data:existing}=await admin.from("quiz_attempt_question_queue").select("question_id,sequence_no").eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",attemptId).order("sequence_no",{ascending:false}),used=new Set((existing||[]).map((x:any)=>x.question_id)),chosen=candidates.find((x:any)=>!used.has(x.id));if(!chosen)return null;const seq=Number(existing?.[0]?.sequence_no||0)+1,{data:inserted}=await admin.from("quiz_attempt_question_queue").insert({workspace_id:WORKSPACE_ID,quiz_attempt_id:attemptId,sequence_no:seq,question_id:chosen.id,source_role:"remediation",parent_question_id:parentQuestionId,concept_id:conceptId,difficulty_level:chosen.difficulty_level,status:"pending",selection_reason:"remediation_after_repeated_error"}).select("sequence_no,question_id,source_role,concept_id,difficulty_level,status").maybeSingle();if(!inserted)return null;const payload=await questionPayload([inserted.question_id]);return{...inserted,question:payload[0]||null};}

async function updateMastery(learnerId:string,conceptId:string|null,q:any,ok:boolean,attemptNo:number,hintsUsed:number,scoreFraction:number){if(!conceptId)return;const{data:old}=await admin.from("learner_concept_mastery").select("id,mastery_score,evidence_count,first_try_correct_count,total_question_count,total_hint_count").eq("workspace_id",WORKSPACE_ID).eq("learner_id",learnerId).eq("concept_id",conceptId).maybeSingle(),e=Number(old?.evidence_count||0),evidenceScore=ok?scoreFraction*100:0,mastery=Math.round(((Number(old?.mastery_score||0)*e)+evidenceScore)/(e+1)*100)/100;await admin.from("learner_concept_mastery").upsert({workspace_id:WORKSPACE_ID,learner_id:learnerId,concept_id:conceptId,mastery_score:mastery,evidence_count:e+1,first_try_correct_count:Number(old?.first_try_correct_count||0)+(ok&&attemptNo===1?1:0),total_question_count:Number(old?.total_question_count||0)+1,total_hint_count:Number(old?.total_hint_count||0)+hintsUsed,last_difficulty:q.difficulty_level,last_assessed_at:new Date().toISOString(),metadata:{engine:"learning-api-v1",latest_attempt_no:attemptNo}},{onConflict:"learner_id,concept_id"});}

async function answerQuestion(learnerId:string,body:any){const attemptId=String(body.attempt_id||""),questionId=String(body.question_id||""),selectedPosition=Number(body.option_position);if(!attemptId||!questionId||!Number.isInteger(selectedPosition))throw new Error("INVALID_ANSWER");const{data:attempt}=await admin.from("quiz_attempts").select("id,learner_id,quiz_version_id,status,delivery_mode").eq("workspace_id",WORKSPACE_ID).eq("id",attemptId).eq("learner_id",learnerId).maybeSingle();if(!attempt||attempt.status!=="in_progress"||attempt.delivery_mode!=="learning")throw new Error("ATTEMPT_NOT_ACTIVE");const{data:queue}=await admin.from("quiz_attempt_question_queue").select("id,sequence_no,question_id,source_role,concept_id,status").eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",attemptId).eq("question_id",questionId).maybeSingle();if(!queue||queue.status==="completed"||queue.status==="skipped")throw new Error("QUESTION_NOT_ACTIVE");const{data:q}=await admin.from("quiz_questions").select("id,question_type,points,difficulty_level,max_attempts,remediation_after_attempt").eq("workspace_id",WORKSPACE_ID).eq("id",questionId).eq("quiz_version_id",attempt.quiz_version_id).maybeSingle();if(!q||q.question_type!=="single_choice")throw new Error("UNSUPPORTED_QUESTION_TYPE");const{data:key}=await admin.from("quiz_question_answer_keys").select("correct_answer,explanation,correct_explanation,final_incorrect_explanation").eq("workspace_id",WORKSPACE_ID).eq("question_id",questionId).maybeSingle();if(!key)throw new Error("ANSWER_KEY_NOT_FOUND");const{count}=await admin.from("quiz_answer_attempts").select("id",{count:"exact",head:true}).eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",attemptId).eq("question_id",questionId),attemptNo=Number(count||0)+1;if(attemptNo>Number(q.max_attempts||4))throw new Error("MAX_ATTEMPTS_REACHED");const correctPosition=Number((key.correct_answer as any)?.option_position),ok=selectedPosition===correctPosition,{data:version}=await admin.from("quiz_versions").select("settings").eq("workspace_id",WORKSPACE_ID).eq("id",attempt.quiz_version_id).single(),scores=Array.isArray((version?.settings as any)?.attempt_scores)?(version!.settings as any).attempt_scores:[100,75,50,25],scoreFraction=ok?Math.max(0,Math.min(1,Number(scores[Math.min(attemptNo-1,scores.length-1)]??25)/100)):0,finalized=ok||attemptNo>=Number(q.max_attempts||4);let hint:any=null,hintLevel:number|null=null;if(!ok&&!finalized){hintLevel=Math.min(attemptNo,4);const{data:h}=await admin.from("quiz_question_hints").select("hint_level,pedagogical_role,content,language,terminology_display_mode").eq("workspace_id",WORKSPACE_ID).eq("question_id",questionId).eq("hint_level",hintLevel).maybeSingle();hint=h||null;}await admin.from("quiz_answer_attempts").insert({workspace_id:WORKSPACE_ID,quiz_attempt_id:attemptId,question_id:questionId,attempt_no:attemptNo,response:{option_position:selectedPosition},is_correct:ok,feedback_text:ok?(key.correct_explanation||key.explanation):(finalized?(key.final_incorrect_explanation||key.explanation):hint?.content||null),hint_level_shown:hintLevel,score_fraction:scoreFraction});let remediation:any=null;if(!ok&&attemptNo===Number(q.remediation_after_attempt||3))remediation=await maybeAddRemediation(attemptId,attempt.quiz_version_id,queue.concept_id,questionId);if(finalized){const hintsUsed=Math.min(attemptNo-(ok?1:0),4),pointsAwarded=Number(q.points||1)*scoreFraction;await admin.from("quiz_attempt_answers").upsert({workspace_id:WORKSPACE_ID,attempt_id:attemptId,question_id:questionId,response:{option_position:selectedPosition},evaluation:ok?"correct":"incorrect",is_correct:ok,points_awarded:pointsAwarded,attempts_used:attemptNo,hints_used:hintsUsed,first_try_correct:ok&&attemptNo===1,mastery_result:ok&&scoreFraction>=.75?"mastered":ok?"needs_practice":"not_mastered"},{onConflict:"attempt_id,question_id"});await admin.from("quiz_attempt_question_queue").update({status:"completed"}).eq("id",queue.id);await updateMastery(learnerId,queue.concept_id,q,ok,attemptNo,hintsUsed,scoreFraction);const{data:next}=await admin.from("quiz_attempt_question_queue").select("id").eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",attemptId).eq("status","pending").order("sequence_no").limit(1).maybeSingle();if(next)await admin.from("quiz_attempt_question_queue").update({status:"active"}).eq("id",next.id);}return{is_correct:ok,attempt_no:attemptNo,finalized,hint,remediation_added:remediation,explanation:finalized?(ok?(key.correct_explanation||key.explanation):(key.final_incorrect_explanation||key.explanation)):null,correct_option_position:finalized?correctPosition:null};}

async function awardCompletion(learnerId:string,quizSlug:string,percentage:number,firstTry:number,hints:number){const{data:prior}=await admin.from("gamification_events").select("id").eq("workspace_id",WORKSPACE_ID).eq("learner_id",learnerId).eq("event_type","quiz_completed").eq("source_type","quiz").eq("source_id",quizSlug).maybeSingle();if(prior)return{already_awarded:true,xp:0,reward_points:0,badges:[]};let xp=25,points=5;if(percentage>=70){xp+=20;points+=5}if(firstTry>=2)xp+=10;if(hints>=2&&percentage>=40)xp+=10;const{data:s0}=await admin.from("learner_gamification_state").select("xp,reward_points,current_streak,longest_streak,last_learning_date").eq("workspace_id",WORKSPACE_ID).eq("learner_id",learnerId).maybeSingle(),old:any=s0||{xp:0,reward_points:0,current_streak:0,longest_streak:0,last_learning_date:null},today=new Date().toISOString().slice(0,10),yesterday=new Date(Date.now()-86400000).toISOString().slice(0,10);let streak=1;if(old.last_learning_date===today)streak=Number(old.current_streak||0);else if(old.last_learning_date===yesterday)streak=Number(old.current_streak||0)+1;const newXp=Number(old.xp||0)+xp,{data:level}=await admin.from("gamification_levels").select("level_no").eq("workspace_id",WORKSPACE_ID).lte("min_xp",newXp).order("min_xp",{ascending:false}).limit(1).maybeSingle();await admin.from("learner_gamification_state").upsert({workspace_id:WORKSPACE_ID,learner_id:learnerId,xp:newXp,reward_points:Number(old.reward_points||0)+points,current_level:Number(level?.level_no||1),current_streak:streak,longest_streak:Math.max(Number(old.longest_streak||0),streak),last_learning_date:today},{onConflict:"learner_id"});await admin.from("gamification_events").insert({workspace_id:WORKSPACE_ID,learner_id:learnerId,event_type:"quiz_completed",xp_delta:xp,reward_points_delta:points,source_type:"quiz",source_id:quizSlug,reason:"Server-graded quiz completion award",metadata:{percentage,first_try_correct:firstTry,hints_used:hints,engine:"learning-api-v1"}});const badgeCodes:string[]=[];if(firstTry>=2)badgeCodes.push("first-try");if(hints>=2&&percentage>=40)badgeCodes.push("keep-going");if(percentage>=85)badgeCodes.push("concept-master");for(const code of badgeCodes){const{data:badge}=await admin.from("gamification_badges").select("id").eq("workspace_id",WORKSPACE_ID).eq("code",code).maybeSingle();if(!badge)continue;const{data:exists}=await admin.from("learner_badges").select("id").eq("learner_id",learnerId).eq("badge_id",badge.id).maybeSingle();if(!exists)await admin.from("learner_badges").insert({workspace_id:WORKSPACE_ID,learner_id:learnerId,badge_id:badge.id,award_reason:`quiz:${quizSlug}`,metadata:{percentage}});}return{already_awarded:false,xp,reward_points:points,badges:badgeCodes};}

async function finishQuiz(learnerId:string,body:any){const attemptId=String(body.attempt_id||""),duration=Math.max(0,Math.min(86400,Number(body.duration_seconds||0))),{data:attempt}=await admin.from("quiz_attempts").select("id,quiz_version_id,status,started_at").eq("workspace_id",WORKSPACE_ID).eq("id",attemptId).eq("learner_id",learnerId).maybeSingle();if(!attempt||attempt.status!=="in_progress")throw new Error("ATTEMPT_NOT_ACTIVE");const{data:open}=await admin.from("quiz_attempt_question_queue").select("id").eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",attemptId).in("status",["pending","active"]).limit(1);if(open?.length)throw new Error("QUIZ_NOT_COMPLETE");const{data:coreQueue}=await admin.from("quiz_attempt_question_queue").select("question_id").eq("workspace_id",WORKSPACE_ID).eq("quiz_attempt_id",attemptId).eq("source_role","core"),coreIds=(coreQueue||[]).map((x:any)=>x.question_id);const[{data:finals},{data:qs},{data:version}]=await Promise.all([admin.from("quiz_attempt_answers").select("question_id,is_correct,points_awarded,first_try_correct,hints_used,response").eq("workspace_id",WORKSPACE_ID).eq("attempt_id",attemptId).in("question_id",coreIds),admin.from("quiz_questions").select("id,points,prompt").eq("workspace_id",WORKSPACE_ID).in("id",coreIds),admin.from("quiz_versions").select("quiz_id").eq("workspace_id",WORKSPACE_ID).eq("id",attempt.quiz_version_id).single()]),maxPoints=(qs||[]).reduce((a:number,q:any)=>a+Number(q.points||0),0),scorePoints=(finals||[]).reduce((a:number,x:any)=>a+Number(x.points_awarded||0),0),percentage=maxPoints>0?Math.round(scorePoints/maxPoints*10000)/100:0,firstTry=(finals||[]).filter((x:any)=>x.first_try_correct).length,hints=(finals||[]).reduce((a:number,x:any)=>a+Number(x.hints_used||0),0);await admin.from("quiz_attempts").update({status:"submitted",submitted_at:new Date().toISOString(),score_points:scorePoints,max_points:maxPoints,percentage,duration_seconds:duration||Math.max(0,Math.round((Date.now()-new Date(attempt.started_at).getTime())/1000)),metadata:{engine:"learning-api-v1",server_graded:true,first_try_correct:firstTry,hints_used:hints}}).eq("id",attemptId);const{data:quiz}=await admin.from("quizzes").select("slug,title").eq("workspace_id",WORKSPACE_ID).eq("id",version!.quiz_id).single(),award=await awardCompletion(learnerId,quiz!.slug,percentage,firstTry,hints),{data:keys}=await admin.from("quiz_question_answer_keys").select("question_id,correct_answer,explanation,correct_explanation,final_incorrect_explanation").eq("workspace_id",WORKSPACE_ID).in("question_id",coreIds),km=new Map((keys||[]).map((k:any)=>[k.question_id,k])),qm=new Map((qs||[]).map((q:any)=>[q.id,q])),review=(finals||[]).map((f:any)=>({question_id:f.question_id,prompt:qm.get(f.question_id)?.prompt,response:f.response,is_correct:f.is_correct,correct_answer:km.get(f.question_id)?.correct_answer,explanation:f.is_correct?(km.get(f.question_id)?.correct_explanation||km.get(f.question_id)?.explanation):(km.get(f.question_id)?.final_incorrect_explanation||km.get(f.question_id)?.explanation)}));return{ok:true,attempt_id:attemptId,quiz,score_points:scorePoints,max_points:maxPoints,percentage,first_try_correct:firstTry,hints_used:hints,award,review};}

Deno.serve(async(req:Request)=>{const origin=req.headers.get("origin");if(req.method==="OPTIONS")return new Response(null,{status:204,headers:cors(origin)});if(req.method!=="POST")return json({error:"METHOD_NOT_ALLOWED"},405,origin);try{const session=await learner(req),body=await req.json().catch(()=>({})),action=String(body.action||"");if(action==="catalog")return json(await catalog(session.learner_id),200,origin);if(action==="start_quiz")return json(await startQuiz(session.learner_id,String(body.quiz_slug||"")),200,origin);if(action==="answer")return json(await answerQuestion(session.learner_id,body),200,origin);if(action==="finish_quiz")return json(await finishQuiz(session.learner_id,body),200,origin);return json({error:"UNKNOWN_ACTION"},400,origin);}catch(e){const msg=e instanceof Error?e.message:"SERVER_ERROR",auth=["AUTH_REQUIRED","INVALID_SESSION","SESSION_EXPIRED"],notFound=["QUIZ_NOT_FOUND","VERSION_NOT_FOUND","QUIZ_NOT_AVAILABLE"],bad=["INVALID_ANSWER","UNSUPPORTED_QUESTION_TYPE","ATTEMPT_NOT_ACTIVE","QUESTION_NOT_ACTIVE","MAX_ATTEMPTS_REACHED","QUIZ_NOT_COMPLETE"],status=auth.includes(msg)?401:notFound.includes(msg)?404:bad.includes(msg)?400:500;return json({error:msg},status,origin);}});
